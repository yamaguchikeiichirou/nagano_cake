<%= render 'public/header' %>
<div class="container">
  <div class="row">
    <div class="col">
      <table class="table table-hover table-inverse">
        <p class="row justify-content-md-center py-5">
          ようこそ、ながのCAKへ！</br>
          このサイトはケーキ販売のECサイトになります。</br>
          会員でない方も商品の閲覧はできますが、</br>
          購入には会員登録が必要になります。
        </p>
        <h3>
          新着商品
        </h3>
        <div class="row">
        <% @items.each do |item| %>
        <div class="col">
          <div class="form-group">
            <% if customer_signed_in? %>
              <%= link_to item_path(item.id),style: "text-decoration: none;" do %>
            　   <% if item.image.attached? %>
              　   <%= image_tag item.image, size: "200x100" %>
            　   <% else %>
              　   <%= image_tag 'no_image', size: "200x100" %>
            　   <% end %>
              <% end %>
            <% else %>
              <% if item.image.attached? %>
                <%= image_tag item.image, size: "200x100" %>
              <% else %>
                <%= image_tag 'no_image', size: "200x100" %>
            　<% end %>
            <% end %>
          </div>
          <div class="form-group">
            <%= item.name %>
          </div>
          <div class="form-group">
            <%= item.price %>
          </div>
        </div>
        <% end %>
        </div>
      </table>
      <div class="row">
        <div class="col text-right">
          <%= link_to "全ての商品をみる>", items_path %>
        </div>
      </div>
    </div>
  </div>
</div>

